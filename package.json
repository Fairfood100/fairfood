{
  "name": "fairfood-cloudflare",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "db:migrate": "wrangler d1 execute fairfood-db --file=./database/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}